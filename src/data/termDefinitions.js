export const termSections = [
  {
    id: 'memory-hierarchy',
    label: 'Memory Hierarchy',
    color: '#3b82f6',
    terms: [
      {
        id: 'mh-1',
        term: 'VAS (Virtual Address Space)',
        definition: 'The illusion that each process has its own private, contiguous memory. The OS and hardware map virtual addresses to physical addresses transparently.',
        category: 'concept',
      },
      {
        id: 'mh-2',
        term: 'Physical Address Space',
        definition: 'The actual hardware memory (RAM) accessed by the CPU. Shared among all processes; the OS manages allocation of physical pages.',
        category: 'concept',
      },
      {
        id: 'mh-3',
        term: 'Word',
        definition: 'The unit of transfer between L1 cache and the CPU. Typically 4 bytes (32-bit) or 8 bytes (64-bit) depending on architecture.',
        category: 'concept',
      },
      {
        id: 'mh-4',
        term: 'Block (cache)',
        definition: 'The unit of transfer between cache levels and main memory. Typically 64 bytes on modern Intel hardware. Also called a cache line.',
        category: 'concept',
      },
      {
        id: 'mh-5',
        term: 'Page',
        definition: 'The unit of transfer between main memory (DRAM) and secondary storage (SSD/HDD). Typically 4KB on modern systems.',
        category: 'concept',
      },
      {
        id: 'mh-6',
        term: 'Latency',
        definition: 'The time delay to access a memory level. Measured in cycles or nanoseconds. Lower levels (registers, L1) have the lowest latency.',
        category: 'concept',
      },
      {
        id: 'mh-7',
        term: 'Cache',
        definition: 'A smaller, faster memory that acts as a staging area for a larger, slower memory. Exploits temporal and spatial locality to reduce average access time.',
        category: 'concept',
      },
      {
        id: 'mh-speed-1',
        term: 'L0 Registers — Speed',
        definition: '1 cycle, ~0.25 ns. Fastest storage; directly used by the CPU. Holds operands for current instructions.',
        category: 'speed',
      },
      {
        id: 'mh-speed-2',
        term: 'L1 Cache — Speed',
        definition: '4 cycles, ~1 ns. On-chip, separate instruction and data caches. 32KB typical per core.',
        category: 'speed',
      },
      {
        id: 'mh-speed-3',
        term: 'L2 Cache — Speed',
        definition: '12 cycles, ~3 ns. Unified on-chip cache. 256KB typical.',
        category: 'speed',
      },
      {
        id: 'mh-speed-4',
        term: 'L3 Cache — Speed',
        definition: '40 cycles, ~10 ns. Shared on-chip cache across cores. 8MB typical.',
        category: 'speed',
      },
      {
        id: 'mh-speed-5',
        term: 'L4 Main Memory (DRAM) — Speed',
        definition: '~100 ns. Off-chip volatile memory. Much slower than any cache level but orders of magnitude larger.',
        category: 'speed',
      },
      {
        id: 'mh-speed-6',
        term: 'L5 SSD — Speed',
        definition: '50–150 μs (microseconds). Non-volatile flash storage. 1000x slower than DRAM.',
        category: 'speed',
      },
      {
        id: 'mh-speed-7',
        term: 'L5 HDD — Speed',
        definition: '1–10 ms (milliseconds). Mechanical disk. Dominated by rotational latency and seek time.',
        category: 'speed',
      },
      {
        id: 'mh-speed-8',
        term: 'L6 Network/Cloud — Speed',
        definition: '~100 ms. Data retrieved from remote servers over a network. Slowest tier of the memory hierarchy.',
        category: 'speed',
      },
    ],
  },
  {
    id: 'c-memory-model',
    label: 'C Memory Model',
    color: '#a78bfa',
    terms: [
      {
        id: 'cm-1',
        term: 'CODE segment (.text / .rodata)',
        definition: 'Stores executable machine code (.text) and read-only constants like string literals (.rodata). Read-only and lives for the entire program lifetime.',
        category: 'segment',
      },
      {
        id: 'cm-2',
        term: 'DATA segment (.data / .bss)',
        definition: 'Stores global and static variables. .data holds initialized globals/statics. .bss holds zero-initialized globals/statics. Lives for entire program lifetime.',
        category: 'segment',
      },
      {
        id: 'cm-3',
        term: 'HEAP segment',
        definition: 'Dynamically allocated memory via malloc/calloc/realloc. Grows upward. Managed by the programmer — must be explicitly freed. Persists until freed or program exits.',
        category: 'segment',
      },
      {
        id: 'cm-4',
        term: 'STACK segment',
        definition: 'Stores local variables, function parameters, and stack frames. Grows downward. Automatically allocated and freed per function scope. Fast but limited size.',
        category: 'segment',
      },
      {
        id: 'cm-5',
        term: 'Global variable placement',
        definition: 'Global variables go in the DATA segment (.data if initialized, .bss if zero-initialized). They live for the entire program lifetime.',
        category: 'placement',
      },
      {
        id: 'cm-6',
        term: 'Static local variable placement',
        definition: 'Static local variables go in the DATA segment, not the stack. They persist across function calls and are initialized only once.',
        category: 'placement',
      },
      {
        id: 'cm-7',
        term: 'Local variable placement',
        definition: 'Local (non-static) variables go on the STACK. They are allocated when the function is called and freed when it returns.',
        category: 'placement',
      },
      {
        id: 'cm-8',
        term: 'malloc\'d memory placement',
        definition: 'Memory allocated with malloc/calloc/realloc lives on the HEAP. Must be explicitly freed with free() to avoid memory leaks.',
        category: 'placement',
      },
      {
        id: 'cm-9',
        term: 'String literals / constants placement',
        definition: 'String literals (e.g., "hello") and constants go in the CODE segment (.rodata). They are read-only; writing to them is undefined behavior.',
        category: 'placement',
      },
      {
        id: 'cm-10',
        term: 'malloc(size)',
        definition: 'void *malloc(size_t size) — Allocates size bytes on the heap. Returns a void pointer to the payload, or NULL on failure. Memory is NOT zero-initialized.',
        category: 'function',
      },
      {
        id: 'cm-11',
        term: 'calloc(n, size)',
        definition: 'void *calloc(size_t n, size_t size) — Allocates n * size bytes on the heap and zeroes all bytes. Returns void* or NULL on failure.',
        category: 'function',
      },
      {
        id: 'cm-12',
        term: 'realloc(ptr, size)',
        definition: 'void *realloc(void *ptr, size_t size) — Resizes an existing heap block. May return a new pointer if the block must move. Returns NULL on failure (original block unchanged).',
        category: 'function',
      },
      {
        id: 'cm-13',
        term: 'free(ptr)',
        definition: 'void free(void *ptr) — Releases a heap block back to the allocator. Calling free on NULL is safe (no-op). Double-free is undefined behavior.',
        category: 'function',
      },
    ],
  },
  {
    id: 'heap-management',
    label: 'Heap Management',
    color: '#f59e0b',
    terms: [
      {
        id: 'hm-1',
        term: 'Block (heap)',
        definition: 'A contiguous chunk of heap memory. Each block has a header and optionally a footer (boundary tag), plus a payload region returned to the programmer.',
        category: 'concept',
      },
      {
        id: 'hm-2',
        term: 'Payload',
        definition: 'The usable part of a heap block given to the programmer. The pointer returned by malloc points to the start of the payload.',
        category: 'concept',
      },
      {
        id: 'hm-3',
        term: 'Overhead',
        definition: 'Header and footer metadata used by the allocator. Not available to the programmer. Reduces memory utilization.',
        category: 'concept',
      },
      {
        id: 'hm-4',
        term: 'Internal fragmentation',
        definition: 'Wasted space inside an allocated block, caused by alignment padding. Example: malloc(13) in an 8-byte-aligned allocator allocates a 16-byte block — 3 bytes wasted.',
        category: 'fragmentation',
      },
      {
        id: 'hm-5',
        term: 'External fragmentation',
        definition: 'Enough total free memory exists but no single free block is large enough for the request. Example: two 16-byte free blocks (non-adjacent) cannot satisfy malloc(32).',
        category: 'fragmentation',
      },
      {
        id: 'hm-6',
        term: 'Throughput',
        definition: 'Number of malloc/free operations completed per unit time. A measure of allocator speed/performance.',
        category: 'metric',
      },
      {
        id: 'hm-7',
        term: 'Memory utilization',
        definition: 'Fraction of heap memory used for payloads. Utilization = sum(payloads) / total_heap_size. Maximized by minimizing fragmentation and overhead.',
        category: 'metric',
      },
      {
        id: 'hm-8',
        term: 'Block header encoding',
        definition: 'bits 31–3: block size | bit 2: unused | bit 1: p-bit (previous block allocated=1, free=0) | bit 0: a-bit (this block allocated=1, free=0). Size is stored in upper bits; lower 3 bits are used for flags since size is always a multiple of 8.',
        category: 'encoding',
      },
      {
        id: 'hm-9',
        term: 'a-bit (allocated bit)',
        definition: 'Bit 0 of a block header. 1 = this block is allocated, 0 = this block is free. Extracted with: header & 0x1. Block size: header & ~0x7.',
        category: 'encoding',
      },
      {
        id: 'hm-10',
        term: 'p-bit (previous-allocated bit)',
        definition: 'Bit 1 of a block header. 1 = the previous block in memory is allocated. 0 = previous block is free. Used to avoid the need for footers on allocated blocks when coalescing.',
        category: 'encoding',
      },
      {
        id: 'hm-11',
        term: 'Header value: 8B allocated, prev allocated',
        definition: '8 | 0b11 = 11. Bits: size=8 (bits 3+), p-bit=1 (bit 1), a-bit=1 (bit 0).',
        category: 'example',
      },
      {
        id: 'hm-12',
        term: 'Header value: 8B allocated, prev free',
        definition: '8 | 0b01 = 9. Bits: size=8, p-bit=0 (prev is free), a-bit=1 (this is allocated).',
        category: 'example',
      },
      {
        id: 'hm-13',
        term: 'Header value: 32B free, prev allocated',
        definition: '32 | 0b10 = 34. Bits: size=32, p-bit=1 (prev is allocated), a-bit=0 (this is free).',
        category: 'example',
      },
      {
        id: 'hm-14',
        term: 'Header value: 64B allocated, prev free',
        definition: '64 | 0b01 = 65. Bits: size=64, p-bit=0 (prev is free), a-bit=1 (this is allocated).',
        category: 'example',
      },
      {
        id: 'hm-15',
        term: 'Implicit Free List',
        definition: 'Every block has a header. To find free blocks, traverse all blocks sequentially. Next block found by: current_address + block_size. Simple but slow for malloc (O(n) total blocks).',
        category: 'structure',
      },
      {
        id: 'hm-16',
        term: 'Explicit Free List',
        definition: 'Only free blocks are tracked in a doubly-linked list. Each free block contains: header, pred pointer, succ pointer, footer. malloc is O(free blocks) instead of O(all blocks). More complex.',
        category: 'structure',
      },
      {
        id: 'hm-17',
        term: 'First Fit (FF) placement',
        definition: 'Start scanning from the beginning of the heap; use the first free block that fits. Fast for small heaps but fragments the beginning of the heap over time.',
        category: 'policy',
      },
      {
        id: 'hm-18',
        term: 'Next Fit (NF) placement',
        definition: 'Start scanning from where the last allocation ended (rover). Faster than first-fit in practice but produces worse fragmentation/utilization.',
        category: 'policy',
      },
      {
        id: 'hm-19',
        term: 'Best Fit (BF) placement',
        definition: 'Scan all free blocks; use the smallest block that fits. Best utilization (least internal fragmentation) but slowest — requires scanning the entire free list.',
        category: 'policy',
      },
      {
        id: 'hm-20',
        term: 'Immediate coalescing',
        definition: 'Adjacent free blocks are merged immediately when a block is freed. Keeps the free list clean but adds work to every free() call.',
        category: 'coalescing',
      },
      {
        id: 'hm-21',
        term: 'Deferred coalescing',
        definition: 'Free blocks are not immediately merged; coalescing is done in batches later. Faster individual free() calls but may cause more fragmentation momentarily.',
        category: 'coalescing',
      },
      {
        id: 'hm-22',
        term: 'Coalescing time complexity',
        definition: 'O(1) constant time when using footers (boundary tags) and p-bits. To find previous block\'s header when it\'s free: read its footer (last word before current header), extract size, subtract.',
        category: 'coalescing',
      },
      {
        id: 'hm-23',
        term: 'Free block footer',
        definition: 'Only free blocks need footers. The footer stores only the block size (same as header). Required to coalesce backward with the previous block in O(1) time.',
        category: 'coalescing',
      },
      {
        id: 'hm-24',
        term: 'Heap caveat: malloc contiguity',
        definition: 'Consecutive malloc calls do NOT produce contiguous payloads. Block headers are inserted between payloads. Never assume two malloc results are adjacent.',
        category: 'caveat',
      },
      {
        id: 'hm-25',
        term: 'Heap caveat: uninitialized memory',
        definition: 'Heap memory (malloc) is NOT initialized to 0. It contains garbage values. Use calloc() if you need zeroed memory.',
        category: 'caveat',
      },
      {
        id: 'hm-26',
        term: 'Dangling pointer',
        definition: 'A pointer that references memory that has already been freed. Dereferencing it is undefined behavior and a common source of bugs and security vulnerabilities.',
        category: 'caveat',
      },
      {
        id: 'hm-27',
        term: 'Double-free',
        definition: 'Calling free() on a pointer that was already freed. Causes undefined behavior, can corrupt the allocator\'s free list, and is an exploitable vulnerability.',
        category: 'caveat',
      },
    ],
  },
  {
    id: 'locality-caching',
    label: 'Locality & Caching',
    color: '#14b8a6',
    terms: [
      {
        id: 'lc-1',
        term: 'Temporal locality',
        definition: 'If a memory address was recently accessed, it will likely be accessed again soon. Example: a loop counter is accessed every iteration.',
        category: 'concept',
      },
      {
        id: 'lc-2',
        term: 'Spatial locality',
        definition: 'If a memory address was recently accessed, nearby addresses will likely be accessed soon. Example: sequential array traversal loads neighboring elements in the same cache block.',
        category: 'concept',
      },
      {
        id: 'lc-3',
        term: 'Stride',
        definition: 'The step size between successive array accesses, measured in words. Stride-1 = sequential (best). Stride-k skips k words per access. Higher stride → worse spatial locality.',
        category: 'concept',
      },
      {
        id: 'lc-4',
        term: 'Cache block (line)',
        definition: 'The unit of memory transferred between cache levels. On modern Intel i7: 64 bytes. All accesses within the same block share one load from the next level.',
        category: 'concept',
      },
      {
        id: 'lc-5',
        term: 'Stride miss rate formula',
        definition: '% misses = min(1, (word_size × k) / B) × 100, where k = stride in words, B = block size in bytes, word_size = bytes per word (typically 4). Stride-1 with B=32: (4×1)/32 = 12.5% miss rate.',
        category: 'formula',
      },
      {
        id: 'lc-6',
        term: 'Row-major order (C)',
        definition: 'C stores 2D arrays in row-major order: A[i][j] is at address base + (i*cols + j)*element_size. Accessing row-by-row (A[i][0], A[i][1]...) is optimal spatial locality.',
        category: 'concept',
      },
      {
        id: 'lc-7',
        term: 'Column-major access on row-major array',
        definition: 'Accessing A[0][j], A[1][j], A[2][j]... in a C 2D array jumps by an entire row width on each access. High stride → poor spatial locality → many cache misses.',
        category: 'concept',
      },
    ],
  },
  {
    id: 'cache-design',
    label: 'Cache Design',
    color: '#ec4899',
    terms: [
      {
        id: 'cd-1',
        term: 'Cache parameters C = (S, E, B, m)',
        definition: 'S = number of sets (= 2^s). E = lines per set (associativity). B = block size in bytes (= 2^b). m = number of address bits.',
        category: 'params',
      },
      {
        id: 'cd-2',
        term: 'Address bit breakdown',
        definition: '| tag (t bits) | set index (s bits) | block offset (b bits) |. t = m - s - b. Tag identifies which block is in the line. Index selects the set. Offset selects a byte within the block.',
        category: 'params',
      },
      {
        id: 'cd-3',
        term: 'b (offset bits)',
        definition: 'b = log₂(B). The number of bits used to address a byte within a cache block. Selects the rightmost b bits of the address.',
        category: 'formula',
      },
      {
        id: 'cd-4',
        term: 's (set index bits)',
        definition: 's = log₂(S). The number of bits used to select which cache set. Extracted from bits b through b+s-1 of the address.',
        category: 'formula',
      },
      {
        id: 'cd-5',
        term: 't (tag bits)',
        definition: 't = m - s - b. The remaining upper bits of the address. Used to identify which memory block is stored in a cache line.',
        category: 'formula',
      },
      {
        id: 'cd-6',
        term: 'Cache size formula',
        definition: 'Cache size = S × E × B bytes. Example: S=64, E=4, B=32 → 64×4×32 = 8192 bytes = 8 KB.',
        category: 'formula',
      },
      {
        id: 'cd-7',
        term: 'Direct Mapped cache',
        definition: 'E=1 line per set. Each memory block maps to exactly one cache line. Fast O(1) lookup but high conflict miss rate.',
        category: 'type',
      },
      {
        id: 'cd-8',
        term: 'Fully Associative cache',
        definition: 'S=1 set containing all E lines. Any block can map to any line. No conflict misses but slow O(E) search. Used for small TLBs.',
        category: 'type',
      },
      {
        id: 'cd-9',
        term: 'Set Associative cache',
        definition: 'S sets with E lines each (E-way set associative). Best of both worlds — fewer conflict misses than direct-mapped, faster than fully associative. Most modern CPUs use this.',
        category: 'type',
      },
      {
        id: 'cd-10',
        term: 'Cold miss (compulsory miss)',
        definition: 'Occurs on the very first access to a memory block. Unavoidable — the block must be loaded into cache for the first time. No cache design can eliminate cold misses.',
        category: 'miss',
      },
      {
        id: 'cd-11',
        term: 'Capacity miss',
        definition: 'Occurs because the cache is too small to hold all the data needed. Even a fully associative cache (no conflicts) would miss. Solution: larger cache.',
        category: 'miss',
      },
      {
        id: 'cd-12',
        term: 'Conflict miss',
        definition: 'Two addresses map to the same cache set and evict each other repeatedly, even if other sets are empty. Eliminated by higher associativity or fully associative design.',
        category: 'miss',
      },
      {
        id: 'cd-13',
        term: 'LRU (Least Recently Used) replacement',
        definition: 'Evict the cache line that was accessed least recently. Tracks last-use time per line. Most effective replacement policy for typical workloads.',
        category: 'replacement',
      },
      {
        id: 'cd-14',
        term: 'LFU (Least Frequently Used) replacement',
        definition: 'Evict the cache line accessed fewest times. Each line has a counter incremented on hit, reset on eviction. Ties broken by LRU. Less common than LRU.',
        category: 'replacement',
      },
      {
        id: 'cd-15',
        term: 'Write-Through policy',
        definition: 'On a cache write hit, update both the cache AND lower-level memory immediately. Keeps memory always consistent. More memory traffic. Paired with No-Write-Allocate.',
        category: 'write',
      },
      {
        id: 'cd-16',
        term: 'Write-Back policy',
        definition: 'On a cache write hit, update cache only. Write to lower memory only when the dirty line is evicted. Less memory traffic. Paired with Write-Allocate.',
        category: 'write',
      },
      {
        id: 'cd-17',
        term: 'Write-Allocate policy',
        definition: 'On a write miss, load the block into cache first, then perform the write in cache. Paired with Write-Back. Exploits future temporal locality.',
        category: 'write',
      },
      {
        id: 'cd-18',
        term: 'No-Write-Allocate policy',
        definition: 'On a write miss, write directly to lower-level memory without loading into cache. Paired with Write-Through. Simpler but less cache-friendly.',
        category: 'write',
      },
      {
        id: 'cd-19',
        term: 'Cache hit rate',
        definition: 'hit rate = hits / total accesses. The fraction of memory accesses served by the cache. Higher is better.',
        category: 'metric',
      },
      {
        id: 'cd-20',
        term: 'Miss penalty',
        definition: 'The extra time required to handle a cache miss — loading the block from the next level of memory. Larger blocks increase miss penalty but improve spatial locality.',
        category: 'metric',
      },
      {
        id: 'cd-21',
        term: 'Effect of larger block size B',
        definition: 'Larger B → higher hit rate (better spatial locality) + longer hit time (more data per line) + higher miss penalty (more bytes to transfer). L1 caches prefer smaller B; L3 prefers larger B.',
        category: 'tradeoff',
      },
      {
        id: 'cd-22',
        term: 'Effect of more sets S',
        definition: 'More sets → higher hit rate (fewer conflict misses) + longer hit time (more SRAM, larger decoder). Same miss penalty.',
        category: 'tradeoff',
      },
      {
        id: 'cd-23',
        term: 'Effect of more lines E (associativity)',
        definition: 'Higher E → higher hit rate (fewer conflict misses) + longer hit time (must check more lines per set). Same miss penalty.',
        category: 'tradeoff',
      },
      {
        id: 'cd-24',
        term: 'Intel i7 L1 cache',
        definition: '32KB instruction cache (4-way), 32KB data cache (8-way). 64-byte blocks. Pseudo-LRU replacement. Write-back.',
        category: 'real',
      },
      {
        id: 'cd-25',
        term: 'Intel i7 L2 cache',
        definition: '256KB, 8-way set associative. 64-byte blocks. Write-back + Write-allocate.',
        category: 'real',
      },
      {
        id: 'cd-26',
        term: 'Intel i7 L3 cache',
        definition: '8MB, 16-way set associative. 64-byte blocks. Write-back + Write-allocate. Shared across all cores.',
        category: 'real',
      },
    ],
  },
  {
    id: 'assembly',
    label: 'Assembly Basics',
    color: '#22c55e',
    terms: [
      {
        id: 'asm-1',
        term: 'Why assembly matters',
        definition: 'Assembly is human-readable machine code. It is machine-specific (IA-32 in CS354). High-level language constructs (loops, variable names, types) do not exist — just registers, memory, and instructions.',
        category: 'concept',
      },
      {
        id: 'asm-2',
        term: 'Assembly suffix: b (byte)',
        definition: 'b = 1 byte (8 bits). C types: char, unsigned char. Register 8-bit halves: %al, %ah, %bl, %bh, %cl, %ch, %dl, %dh. Example: movb $0, %al',
        category: 'suffix',
      },
      {
        id: 'asm-3',
        term: 'Assembly suffix: w (word)',
        definition: 'w = 2 bytes (16 bits). C types: short, unsigned short. Register 16-bit aliases: %ax, %bx, %cx, %dx, %si, %di, %sp, %bp. Example: movw %ax, %bx',
        category: 'suffix',
      },
      {
        id: 'asm-4',
        term: 'Assembly suffix: l (long/double-word)',
        definition: 'l = 4 bytes (32 bits). C types: int, unsigned int, long, unsigned long, float, any pointer. Most common suffix in IA-32. Register 32-bit names: %eax, %ebx, %ecx, %edx, %esi, %edi, %esp, %ebp. Example: movl %eax, %ebx',
        category: 'suffix',
      },
      {
        id: 'asm-5',
        term: '%eax register',
        definition: 'Accumulator register. Holds function return values. Used for arithmetic and most operations. 32-bit; 16-bit alias %ax; 8-bit halves %ah (high byte) and %al (low byte).',
        category: 'register',
      },
      {
        id: 'asm-6',
        term: '%ecx register',
        definition: 'Counter register. Often used as a loop counter and for shift/rotate counts. 32-bit; 16-bit alias %cx; 8-bit halves %ch, %cl.',
        category: 'register',
      },
      {
        id: 'asm-7',
        term: '%edx register',
        definition: 'Data register. Used for I/O operations and holds the upper 32 bits of 64-bit multiply/divide results (alongside %eax). 32-bit; 16-bit alias %dx; 8-bit halves %dh, %dl.',
        category: 'register',
      },
      {
        id: 'asm-8',
        term: '%ebx register',
        definition: 'Base register. Often used to hold base addresses of data structures. Callee-saved (must be preserved across function calls). 32-bit; 16-bit alias %bx; 8-bit halves %bh, %bl.',
        category: 'register',
      },
      {
        id: 'asm-9',
        term: '%esi / %edi registers',
        definition: '%esi = source index, %edi = destination index. Traditionally used for string/memory copy operations. Also used as general-purpose registers. 16-bit aliases %si, %di.',
        category: 'register',
      },
      {
        id: 'asm-10',
        term: '%esp register',
        definition: 'Stack pointer. Always points to the top of the stack (lowest address of the active stack frame). Decremented by pushl, incremented by popl. Do not modify directly unless managing frames manually.',
        category: 'register',
      },
      {
        id: 'asm-11',
        term: '%ebp register',
        definition: 'Base pointer (frame pointer). Points to the base of the current stack frame. Local variables accessed at negative offsets (e.g., -4(%ebp)); function parameters at positive offsets (e.g., 8(%ebp)).',
        category: 'register',
      },
      {
        id: 'asm-12',
        term: '%eip register',
        definition: 'Instruction pointer (program counter). Points to the next instruction to execute. Cannot be read or written directly — modified implicitly by call, ret, and jmp instructions.',
        category: 'register',
      },
      {
        id: 'asm-13',
        term: 'Immediate operand ($Imm)',
        definition: 'A literal constant value embedded in the instruction, prefixed with $. Example: $5 is the value 5. Example: movl $42, %eax loads 42 into %eax. Immediates are NOT memory addresses — they are the value itself.',
        category: 'operand',
      },
      {
        id: 'asm-21',
        term: 'leal instruction',
        definition: 'leal src, dst — Load Effective Address. Computes the address described by src and stores that ADDRESS (not the value at the address) into dst. Used for pointer arithmetic and fast multiply-by-constant. Example: leal 8(%eax,%ebx,4), %ecx computes %eax + %ebx*4 + 8 and stores the result in %ecx — no memory access occurs.',
        category: 'instruction',
      },
    ],
  },
  {
    id: 'formulas',
    label: 'Key Formulas',
    color: '#f97316',
    terms: [
      {
        id: 'f-1',
        term: 'b = log₂(B)',
        definition: 'Number of block offset bits. B = cache block size in bytes. Example: B=64 → b=6 offset bits.',
        category: 'formula',
      },
      {
        id: 'f-2',
        term: 's = log₂(S)',
        definition: 'Number of set index bits. S = number of sets. Example: S=32 → s=5 index bits.',
        category: 'formula',
      },
      {
        id: 'f-3',
        term: 't = m - s - b',
        definition: 'Number of tag bits. m = total address bits. Remaining bits after subtracting index and offset bits. Example: m=32, s=5, b=6 → t=21 tag bits.',
        category: 'formula',
      },
      {
        id: 'f-4',
        term: 'Cache size = S × E × B',
        definition: 'Total bytes of cache data storage (not including tag/valid bit overhead). Example: S=64, E=4, B=64 → 64×4×64 = 16,384 bytes = 16 KB.',
        category: 'formula',
      },
      {
        id: 'f-5',
        term: '% misses = min(1, (4×k)/B) × 100',
        definition: 'Stride miss rate formula for 4-byte words. k = stride in words, B = block size in bytes. Example: k=8, B=32 → min(1, 32/32)×100 = 100% miss rate.',
        category: 'formula',
      },
      {
        id: 'f-6',
        term: 'M = 2^m',
        definition: 'Total addressable memory (address space size). m = address bits. Example: m=32 → 2^32 = 4 GB address space.',
        category: 'formula',
      },
      {
        id: 'f-7',
        term: 'Block size from header: header & ~0x7',
        definition: 'Masks off the lower 3 flag bits to extract block size. Example: header=0x31=49 → 49 & ~7 = 48. Block size = 48 bytes.',
        category: 'formula',
      },
      {
        id: 'f-8',
        term: 'Allocated bit from header: header & 0x1',
        definition: 'Masks to the lowest bit (a-bit). 1 = allocated, 0 = free. Example: header=0x31=49 → 49 & 1 = 1 → allocated.',
        category: 'formula',
      },
      {
        id: 'f-9',
        term: 'Memory utilization',
        definition: 'Utilization = Σ(payload sizes) / total_heap_size. Ranges from 0 to 1. Lower fragmentation and overhead → higher utilization.',
        category: 'formula',
      },
      {
        id: 'f-10',
        term: 'Next block address (implicit list)',
        definition: 'next_block = current_block_address + block_size. Block size extracted from the current block\'s header.',
        category: 'formula',
      },
    ],
  },
]

export const allTerms = termSections.flatMap(s => s.terms.map(t => ({ ...t, sectionId: s.id, sectionLabel: s.label, sectionColor: s.color })))
